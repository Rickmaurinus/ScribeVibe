# PyTorch with CUDA 12.1 — install before everything else:
# pip install "torch>=2.5.0,<3" --extra-index-url https://download.pytorch.org/whl/cu121

faster-whisper>=1.0.3,<2
numpy>=1.26.4,<3
sounddevice>=0.5.5,<1
soxr>=1.0.0,<2
PySide6>=6.6.0,<7
pyqtgraph>=0.13.3,<1
pywin32>=311
Pillow>=10.0.0,<13
pystray>=0.19.5,<1

# To generate a fully-pinned lockfile for reproducible installs:
# pip install pip-tools
# pip-compile requirements.txt --output-file requirements.lock
